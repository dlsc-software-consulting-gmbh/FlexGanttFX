This demo focuses on the lighter `GanttChartLite` variant. It shows the same core scheduling concepts as the full control, but in a more compact setup for cases where a simpler container is enough.

```java
GanttChartLite<DemoRow> gc = new GanttChartLite<>();
```