This demo focuses on an empty chart setup. It is useful for seeing the minimal structure of a `GanttChart` before rows and activities are added, and for understanding the blank-state behavior of the control.

```java
GanttChart<DemoRow> gc = new GanttChart<>();
```
